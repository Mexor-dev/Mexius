[package]
name = "zeroclaw-memory"
version = "0.1.0"
edition = "2021"

[lib]
name = "zeroclaw_memory"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
 
